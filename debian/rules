#!/usr/bin/make -f

clean:
	cd pc-navtex; QT_SELECT=5 qmake; make clean; cd ..

build:
	cd pc-navtex; QT_SELECT=5 qmake; make; cd ..

build-arch: build

build-indep: build

binary-arch: build-arch

binary-indep: build-indep

binary: binary-arch binary-indep
